# Changelog

## [2026-08-27]

1. **修复增量同步误删嵌套目录映射下文件的问题**
    当多个路径映射存在父子重叠（如同时映射 `/media` 和 `/media/sub`）时，增量同步会在扫描父目录阶段把子目录映射的存量 STRM 误判为已删除并清理，导致文件反复删除重建。现在删除判定改为对全部路径映射逐一精确匹配，重叠映射不再互相干扰。

2. **修复客户端缓存旧播放信息的问题**
    代理改写后的响应保留了上游的缓存校验头，部分客户端会命中本地缓存继续使用旧数据，导致反复转码或配置不生效。现在改写响应时会剥离这些校验头，客户端始终拿到最新内容。

3. **提升 115 接口调用稳定性**
    SDK 网络调用增加默认超时（连接 10 秒、读取 60 秒），避免个别请求无限等待；目录遍历字段解析统一走 SDK 规范化接口；302 跳转服务减少正常路径的日志输出，错误日志补充完整堆栈。

4. **修复打包缺少二维码库的问题**
    CI 构建环境补充 qrcode 依赖声明，修复打包后的 exe 扫码登录显示"获取失败"的问题。

5. **Release 说明自动生成**
    发版时 GitHub Release 页面的更新说明自动取自 CHANGELOG 最新条目，与发版内容保持一致。

6. **同步不再卡住管理界面**
    之前点击"全量同步"后，同步期间管理页面和托盘都无响应、进度条也不动，只能等整场同步结束。现在同步任务改为后台运行，点击后立即返回，进度条实时刷新，同步过程中其他功能可正常使用。

7. **修复更换 Cookie 或跳转地址后同步不生效的问题**
    之前保存新的 115 Cookie 或修改 STRM 跳转地址前缀后，同步仍沿用旧配置，必须重启程序才生效。现在每次同步都会使用最新的配置。

8. **修复网盘文件同时被移动又发生变化时媒体库出现重复条目**
    增量同步时，如果某个文件既被移动到别的目录、内容又发生了变化，旧路径下的 STRM 文件会残留，导致媒体库里出现重复条目。现在会自动清理旧路径的 STRM 文件。

9. **修复过期下载地址仍被分发的问题**
    之前 115 返回的下载地址临近过期时，仍可能被缓存并分发给后续请求，导致播放中途失败。现在缓存时间不会超过地址的实际剩余有效期，即将过期的地址不再缓存。

## [2026-08-20]

1. **多个客户端同时播放同一视频时不再重复获取下载地址**
    之前多个客户端同时播放同一个视频，会各自向网盘获取下载地址，容易触发网盘接口限流。现在同一时刻只会获取一次，其余客户端直接复用缓存结果，播放更稳定。

2. **增量同步支持网盘内文件移动和重命名**
    之前文件在网盘里移动或重命名后，本地 STRM 不会跟着更新，只能等全量同步才能修正。现在增量同步会自动把本地 STRM 迁移到新位置，并更新对应记录。

## [2026-07-31]

1. **更新依赖库版本**
    p115client 升级到 0.0.9.6.5.1，full_strm_sync 升级到 0.1.6，全部平台的打包依赖同步更新。

2. **目录浏览字段解析改用统一接口**
    目录浏览功能改用 p115client 的字段规范化接口解析网盘返回数据，字段映射与底层客户端保持同步，并新增对应单元测试。

3. **新增目录浏览单元测试**
    覆盖目录过滤、文件排除及 fc 字段类型兼容场景，全量测试通过。

4. **修复扫码登录无法显示二维码的问题**
    增加 qrcode 和 Pillow 依赖声明，打包时自动包含二维码生成库，修复扫码登录弹窗始终显示"获取失败"的问题。

5. **修复保存 Cookie 后不生效的问题**
    在管理界面保存 115 Cookie 后自动重建客户端，Cookie 不再需要重启程序才能生效。

6. **优化扫码登录提示**
    扫码弹窗和 README 增加提示：优先使用微信/支付宝小程序端扫码，网页端易触发 115 的"IP 登录异常"限制。

## [2026-07-22]

1. **文件遍历改用新接口，避免同步失败**
    文件同步改用新的接口方式，避免因网盘接口限制导致同步无法进行的问题。同时修复了目录浏览功能。

2. **修复某些播放器无法播放含中文文件名视频的问题**
    优化了视频跳转地址的处理方式，含中文名的视频现在可以正常播放了。

3. **更新依赖库版本**
    底层依赖库更新到最新版本。

4. **新增 302 直链模式开关**
    管理界面 Emby 配置新增"302 直链模式"开关，默认关闭。关闭时走老的流式代理，开启时走 302 直链（客户端直连网盘下载，不经过服务器中转）。

5. **修复 302 直链模式下拖拽进度卡顿问题**
    优化了跳转地址的解析方式，现在拖拽播放进度不会再卡顿了。

6. **修复部分客户端（如王二小放牛娃）直链播放失败问题**
    优化了播放信息处理，更多客户端可以正常使用 302 直链播放。

7. **修复一个概率性网络协议错误**
    修复了某些情况下请求出错的问题。

8. **全量同步后清理残留文件（默认关闭，需手动开启）**
    全量同步完成后，自动删除目标目录中已不存在的 STRM 文件。默认关闭，需在管理界面中手动开启。

9. **防止重复点击同步导致数据混乱**
    同步过程中再次点击同步按钮会被忽略，避免同时运行多个同步任务导致冲突。

10. **同步进度实时显示**
    同步页面新增进度条，实时显示扫描进度和当前状态，完成后自动刷新记录。

11. **优化批量写入稳定性**
    优化了数据库写入方式，避免同步大量文件时出现数据不一致的问题。

12. **取消同步实时反馈**
    点击取消同步后，进度条显示"正在取消..."，取消完成后自动弹出提示。

## [2026-07-21]

1. **播放流量不再经过服务器**
    代理改为 302 重定向，客户端直连 115 CDN 下载视频，不再占用服务器带宽。

2. **修复 302 导致浏览器黑屏的问题**
    - 去掉下载头，浏览器不再把视频播放当下载处理。
    - 下载地址获取方式优化，确保浏览器能正常播放。

3. **优化下载地址获取稳定性**
    优化了下载地址获取逻辑，遇到网络波动时自动重试，播放更稳定。

4. **精简跳转服务代码**
    简化了内部跳转逻辑，提升响应速度。

5. **版本号更正**
    错误提示里的版本号显示不正确，已修正。

6. **文档全部刷新**
    所有文档更新，与最新代码保持一致。
