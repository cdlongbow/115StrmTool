# Changelog

## [2026-08-31]

1. **修复全量与增量同步始终失败的问题**
    上个版本为 SDK 网络调用加入的默认超时配置与底层网络库的参数格式不兼容，同步在解析网盘目录第一步就报错终止，全量和增量同步一个文件都无法生成。现在超时参数改为底层库兼容的格式，同步恢复正常，连接 10 秒、读取 60 秒的超时保护继续有效。

2. **下载接口被风控时立即降级，缩短播放等待**
    之前 115 下载接口被风控（返回 405）时，程序内部引用了一个未定义的变量导致降级逻辑中断，要白跑 3 轮重试才会切到 SDK 通道，播放前等待时间明显变长。现在收到 405 直接降级，不再多余等待。

3. **修复同步扫描目录时报 KeyError 的问题**
    超时问题修复后同步得以继续执行，随即暴露出目录遍历环节的另一个问题：SDK 返回的文件信息被重复做了两次字段规范化，第二次解析时格式无法识别，扫描第一步就报 KeyError: 'fid'，同步仍然一个文件都无法生成。现在去掉重复处理，文件信息由 SDK 规范化一次后直接使用，全量和增量同步恢复正常。

4. **修复重置同步数据后页面显示不更新的问题**
    点击"重置同步数据"成功后，页面下方的同步记录和仪表盘的已同步文件数仍显示旧内容，需要手动刷新页面才能看到变化。现在重置完成后会自动刷新这两处显示。

## [2026-08-30]

1. **CI 打包前自动运行全部单元测试**
    自动构建流程现在先跑完 113 个测试用例再打包 exe，测试失败会中止构建，避免带问题的版本被发布。

2. **补全离线依赖包**
    离线依赖目录补充 p115oss、python-concurrenttools 两个 115 SDK 的必需组件，纯离线环境安装 wheels 不再缺包。

3. **修复签到结果误报的问题**
    之前只要 115 接口有响应就算签到成功，即使接口明确返回失败（如风控拦截）也会提示"签到成功"。现在只有接口确认签到生效才记录成功，失败会如实提示原因并按原计划重试。

4. **修复目录浏览只显示第一页的问题**
    115 目录下子目录超过 1000 个时，管理界面的目录浏览器只显示前 1000 个。现在自动翻页加载全部子目录，同时浏览接口改为后台执行，不再阻塞其他页面操作。

5. **管理界面默认只监听本机，Cookie 不再明文回显**
    管理界面默认只允许本机访问（如需局域网访问可在设置中手动改为 0.0.0.0）；配置页的 115 Cookie 保存后以掩码显示，界面和日志中不再出现完整 Cookie，重新输入完整 Cookie 即可更换。

6. **修复端口被占用时服务状态误报"运行中"的问题**
    之前 Emby 代理或 302 跳转服务的端口被其他程序占用时，启动会静默失败但界面仍显示"运行中"。现在启动前会预检端口，被占用时明确提示失败原因。

7. **修复更换 Cookie 后旧连接未释放的问题**
    在管理界面更新 115 Cookie 后，旧的网盘连接会被正常关闭，避免长时间运行多次更换后连接累积。

8. **修复播放中途卡顿概率增加的问题**
    302 直链解析失败时（如 115 接口超时），之前坏结果会被缓存较长时间，导致后续播放反复拿到失效地址。现在失败结果只缓存 5 秒，快速恢复。

9. **下载接口被 115 风控时自动降级**
    115 下载接口被风控（返回 405）时，取下载地址自动降级走 SDK 内置通道，播放不再直接失败。

10. **临近过期的下载地址提前告警**
    302 跳转分发的地址临近过期时记录告警日志，便于排查"播放中途失败"类问题。

11. **WebSocket 断开时彻底释放代理资源**
    Emby WebSocket 单向断开时双向连接都会立即关闭，避免残留连接堆积。

12. **115 目录名含特殊字符时浏览器不再异常**
    目录浏览器改用安全的事件绑定，包含引号等特殊字符的目录名可正常进入，同时单击选中、双击进入子目录，交互更明确。

13. **Rust 加速模式的媒体格式配置热更新生效**
    在管理界面修改媒体文件扩展名后，Rust 加速核心会自动重建，无需重启程序。

14. **修复路径映射异常时 STRM 生成到错误位置的问题**
    同步过程中发现网盘路径与映射目录不符的文件（理论上由异常数据导致）会跳过并记录告警，不再拼出越界的本地路径。

15. **附属文件下载逻辑统一**
    全量与增量同步共用同一附属文件（字幕、封面等）下载实现，行为一致且失败时提示更明确。

16. **依赖声明补全**
    requirements.txt 补全 p115 系列 SDK 依赖并移除重复项，新环境安装不再遗漏。

## [2026-08-27]

1. **修复增量同步误删嵌套目录映射下文件的问题**
    当多个路径映射存在父子重叠（如同时映射 `/media` 和 `/media/sub`）时，增量同步会在扫描父目录阶段把子目录映射的存量 STRM 误判为已删除并清理，导致文件反复删除重建。现在删除判定改为对全部路径映射逐一精确匹配，重叠映射不再互相干扰。

2. **修复客户端缓存旧播放信息的问题**
    代理改写后的响应保留了上游的缓存校验头，部分客户端会命中本地缓存继续使用旧数据，导致反复转码或配置不生效。现在改写响应时会剥离这些校验头，客户端始终拿到最新内容。

3. **提升 115 接口调用稳定性**
    SDK 网络调用增加默认超时（连接 10 秒、读取 60 秒），避免个别请求无限等待；目录遍历字段解析统一走 SDK 规范化接口；302 跳转服务减少正常路径的日志输出，错误日志补充完整堆栈。

4. **修复打包缺少二维码库的问题**
    CI 构建环境补充 qrcode 依赖声明，修复打包后的 exe 扫码登录显示"获取失败"的问题。

5. **Release 说明自动生成**
    发版时 GitHub Release 页面的更新说明自动取自 CHANGELOG 最新条目，与发版内容保持一致。

6. **同步不再卡住管理界面**
    之前点击"全量同步"后，同步期间管理页面和托盘都无响应、进度条也不动，只能等整场同步结束。现在同步任务改为后台运行，点击后立即返回，进度条实时刷新，同步过程中其他功能可正常使用。

7. **修复更换 Cookie 或跳转地址后同步不生效的问题**
    之前保存新的 115 Cookie 或修改 STRM 跳转地址前缀后，同步仍沿用旧配置，必须重启程序才生效。现在每次同步都会使用最新的配置。

8. **修复网盘文件同时被移动又发生变化时媒体库出现重复条目**
    增量同步时，如果某个文件既被移动到别的目录、内容又发生了变化，旧路径下的 STRM 文件会残留，导致媒体库里出现重复条目。现在会自动清理旧路径的 STRM 文件。

9. **修复过期下载地址仍被分发的问题**
    之前 115 返回的下载地址临近过期时，仍可能被缓存并分发给后续请求，导致播放中途失败。现在缓存时间不会超过地址的实际剩余有效期，即将过期的地址不再缓存。

10. **修复清空日志后日志文件出现空白段的问题**
    之前清空日志后程序仍按原位置继续写入，日志文件会出现大段空白。现在直接重置日志文件句柄，清空真正生效。

11. **修复托盘菜单操作失效的问题**
    修改过管理界面端口后，Windows 托盘菜单的"全量同步 / 增量同步 / 立即签到"仍固定访问 8100 端口而失效，现在会跟随实际配置的端口；托盘组件缺失时也不再卡死，自动回退到控制台模式运行。

12. **扫码登录的 Cookie 现在会自动保存**
    之前扫码登录成功后 Cookie 只保存在内存里，重启程序就要重新登录。现在扫码成功后 Cookie 会自动写入配置文件并加密保存。

13. **减少播放请求的日志噪音**
    每次播放都会打印一条 302 直链日志，日志文件增长很快。该日志已降为调试级别，默认配置下不再产生，排查问题时调低日志级别即可看到。

14. **提升配置和签到状态的写入可靠性**
    配置文件和签到状态文件改为先写临时文件再替换，程序在写入过程中意外退出不会再产生损坏的配置文件。

15. **修复签到时间范围设置被忽略时报错的问题**
    签到时间范围设置成结束时间早于开始时间（如 23:00-06:00）时，之前会抛出异常。现在自动回退到默认时间范围（06:00-09:00）。

16. **新增 Rust 加速生成 STRM 开关**
    在 115 设置中可开启 Rust 加速，由 Rust 加速核心批量生成 STRM 文件，大库同步速度更快。加速核心缺失、批处理失败或部分文件失败时，自动用内置方式补写对应 STRM 文件，不影响同步结果。

17. **清理不再使用的废弃接口**
    移除了从未对外启用的分享转存、离线下载与清空 STRM 等遗留接口及相关数据表，缩减程序体积，现有功能不受影响。

18. **修复"选择目录"弹窗卡死时程序被冻结的问题**
    在部分 Windows 环境下，点击"选择本地目录"弹出的系统选择框可能无法显示且导致整个程序无响应。现在该弹窗在独立线程中打开并带有超时保护，一旦长时间无响应会自动关闭并回退到手动输入路径，不会再卡住程序。

## [2026-08-20]

1. **多个客户端同时播放同一视频时不再重复获取下载地址**
    之前多个客户端同时播放同一个视频，会各自向网盘获取下载地址，容易触发网盘接口限流。现在同一时刻只会获取一次，其余客户端直接复用缓存结果，播放更稳定。

2. **增量同步支持网盘内文件移动和重命名**
    之前文件在网盘里移动或重命名后，本地 STRM 不会跟着更新，只能等全量同步才能修正。现在增量同步会自动把本地 STRM 迁移到新位置，并更新对应记录。

## [2026-07-31]

1. **更新依赖库版本**
    p115client 升级到 0.0.9.6.5.1，full_strm_sync 升级到 0.1.6，全部平台的打包依赖同步更新。

2. **目录浏览字段解析改用统一接口**
    目录浏览功能改用 p115client 的字段规范化接口解析网盘返回数据，字段映射与底层客户端保持同步，并新增对应单元测试。

3. **新增目录浏览单元测试**
    覆盖目录过滤、文件排除及 fc 字段类型兼容场景，全量测试通过。

4. **修复扫码登录无法显示二维码的问题**
    增加 qrcode 和 Pillow 依赖声明，打包时自动包含二维码生成库，修复扫码登录弹窗始终显示"获取失败"的问题。

5. **修复保存 Cookie 后不生效的问题**
    在管理界面保存 115 Cookie 后自动重建客户端，Cookie 不再需要重启程序才能生效。

6. **优化扫码登录提示**
    扫码弹窗和 README 增加提示：优先使用微信/支付宝小程序端扫码，网页端易触发 115 的"IP 登录异常"限制。

## [2026-07-22]

1. **文件遍历改用新接口，避免同步失败**
    文件同步改用新的接口方式，避免因网盘接口限制导致同步无法进行的问题。同时修复了目录浏览功能。

2. **修复某些播放器无法播放含中文文件名视频的问题**
    优化了视频跳转地址的处理方式，含中文名的视频现在可以正常播放了。

3. **更新依赖库版本**
    底层依赖库更新到最新版本。

4. **新增 302 直链模式开关**
    管理界面 Emby 配置新增"302 直链模式"开关，默认关闭。关闭时走老的流式代理，开启时走 302 直链（客户端直连网盘下载，不经过服务器中转）。

5. **修复 302 直链模式下拖拽进度卡顿问题**
    优化了跳转地址的解析方式，现在拖拽播放进度不会再卡顿了。

6. **修复部分客户端（如王二小放牛娃）直链播放失败问题**
    优化了播放信息处理，更多客户端可以正常使用 302 直链播放。

7. **修复一个概率性网络协议错误**
    修复了某些情况下请求出错的问题。

8. **全量同步后清理残留文件（默认关闭，需手动开启）**
    全量同步完成后，自动删除目标目录中已不存在的 STRM 文件。默认关闭，需在管理界面中手动开启。

9. **防止重复点击同步导致数据混乱**
    同步过程中再次点击同步按钮会被忽略，避免同时运行多个同步任务导致冲突。

10. **同步进度实时显示**
    同步页面新增进度条，实时显示扫描进度和当前状态，完成后自动刷新记录。

11. **优化批量写入稳定性**
    优化了数据库写入方式，避免同步大量文件时出现数据不一致的问题。

12. **取消同步实时反馈**
    点击取消同步后，进度条显示"正在取消..."，取消完成后自动弹出提示。

## [2026-07-21]

1. **播放流量不再经过服务器**
    代理改为 302 重定向，客户端直连 115 CDN 下载视频，不再占用服务器带宽。

2. **修复 302 导致浏览器黑屏的问题**
    - 去掉下载头，浏览器不再把视频播放当下载处理。
    - 下载地址获取方式优化，确保浏览器能正常播放。

3. **优化下载地址获取稳定性**
    优化了下载地址获取逻辑，遇到网络波动时自动重试，播放更稳定。

4. **精简跳转服务代码**
    简化了内部跳转逻辑，提升响应速度。

5. **版本号更正**
    错误提示里的版本号显示不正确，已修正。

6. **文档全部刷新**
    所有文档更新，与最新代码保持一致。
